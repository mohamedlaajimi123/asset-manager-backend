export interface ThemePalette {
  background: string;
  panelLeft: string;
  primaryText: string;
  mutedText: string;
  card: string;
  border: string;
  input: string;
  accent: string;
  accentHover: string;
  errorBg: string;
  errorBorder: string;
  errorText: string;
}